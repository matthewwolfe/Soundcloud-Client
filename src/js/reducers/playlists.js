const initialState = [];

function playlist(state, action){
    switch(action.type){
        default:
            return state;
    }
}

export function playlists(state = initialState, action){
    switch(action.type){
        default:
            return state;
    }
}